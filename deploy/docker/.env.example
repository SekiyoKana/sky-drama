# Optional mirror and build settings for restricted networks.
# Copy this file to deploy/docker/.env and adjust values.

# Docker base images (default: docker.io)
PYTHON_BASE_IMAGE=python:3.12-slim
NODE_BASE_IMAGE=node:20-alpine
NGINX_BASE_IMAGE=nginx:alpine
# Example with a mirror prefix (if your network blocks docker.io):
# PYTHON_BASE_IMAGE=docker.m.daocloud.io/python:3.12-slim
# NODE_BASE_IMAGE=docker.m.daocloud.io/node:20-alpine
# NGINX_BASE_IMAGE=docker.m.daocloud.io/nginx:alpine

# Optional package mirrors inside image build
APT_MIRROR=
PIP_INDEX_URL=
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,backend

# Frontend API path at build time
VITE_API_URL=/v1

# Optional host published ports
BACKEND_PORT=11451
FRONTEND_PORT=5173
